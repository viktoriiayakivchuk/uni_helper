import 'package:flutter/material.dart';

class SchedulePage extends StatelessWidget { // ПЕРЕВІР ЦЮ НАЗВУ
  const SchedulePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Сторінка розкладу'));
  }
}